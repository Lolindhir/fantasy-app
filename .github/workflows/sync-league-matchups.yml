name: Data • League Matchups

on:
  repository_dispatch:
    types:
      - scheduler-sync-league-matchups
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-matchups:
    uses: ./.github/workflows/sync-league-source-data.yml
    with:
      materialization_scope: matchups
    secrets: inherit
